#include <iostream>
#include <boost/regex.hpp>
#include <cmath>
#include <thread>

namespace TextCalc {

    void MultOrDiv(std::string &text);
    void SumOrDiff(std::string &text);
    void MathFunc(std::string &text);

    void Do(std::string &text);

};